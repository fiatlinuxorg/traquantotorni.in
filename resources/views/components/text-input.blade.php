<div class="mb-2 md:mb-4">
    <label for="{{ $name }}" class="block text-xs md:text-sm font-medium text-dark">{{ $label }}</label>
    <input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" class="mt-1 focus:ring-primary focus:border-primary block w-full shadow-sm text-xs sm:text-sm border-gray rounded-md p-2">
</div>