<div class="mb-4">
    <label for="{{ $name }}" class="block text-sm font-medium text-dark">{{ $label }}</label>
    <input id="{{ $name }}" name="{{ $name }}" type="{{ $type }}" class="mt-1 focus:ring-primary focus:border-primary block w-full shadow-sm sm:text-sm border-gray rounded-md">
</div>